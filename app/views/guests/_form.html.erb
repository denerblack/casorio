<%= form_for @guest, :html => { :class => 'form-horizontal' } do |f| %>
  <div class="control-group">
    <%= f.label :user_id, :class => 'control-label' %>
    <div class="controls">
      <%= f.select :user_id, options_from_collection_for_select(User.all, :id, :email), :class => 'text_field', current_user.id %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :name, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :name, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :companions_amount, :class => 'control-label' %>
   <div class="controls">
      <%= f.number_field :companions_amount, :class => 'number_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :children_amount, :class => 'control-label' %>
    <div class="controls">
      <%= f.number_field :children_amount, :class => 'number_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :phone_number, :class => 'control-label' %>
    <div class="controls">
      <%= f.number_field :phone_number, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :confirmed, :class => 'control-label' %>
    <div class="controls">
      <%= f.check_box :confirmed, :class => 'check_box' %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                guests_path, :class => 'btn' %>
  </div>
<% end %>
